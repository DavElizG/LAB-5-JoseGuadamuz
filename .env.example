# ===========================================
# UNA-CHAT - Configuración de Entorno
# ===========================================
# IMPORTANTE: NO SUBIR ESTE ARCHIVO A GIT
# Copia este archivo como .env y configura tus valores

# ===========================================
# Servidor
# ===========================================
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000

# ===========================================
# Auth0 Configuration
# ===========================================
# Obtén estos valores desde tu dashboard de Auth0:
# https://manage.auth0.com/

# Domain de tu tenant de Auth0
# Ejemplo: dev-abc123.us.auth0.com
AUTH0_DOMAIN=your-tenant.auth0.com

# Client ID de tu aplicación
AUTH0_CLIENT_ID=your_client_id_here

# Client Secret de tu aplicación
AUTH0_CLIENT_SECRET=your_client_secret_here

# Callback URL después de autenticación
# Debe estar configurada en Auth0 Dashboard > Applications > Settings > Allowed Callback URLs
AUTH0_CALLBACK_URL=http://localhost:3000/callback

# Logout redirect URL
# Debe estar configurada en Auth0 Dashboard > Applications > Settings > Allowed Logout URLs
AUTH0_LOGOUT_URL=http://localhost:3000

# Audience (opcional) - Solo si usas APIs de Auth0
# AUTH0_AUDIENCE=https://your-api-identifier

# ===========================================
# Session Configuration
# ===========================================
# CRÍTICO: Genera un string aleatorio largo y seguro
# Ejemplo de generación: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=cambia_este_secreto_en_produccion

# ===========================================
# CORS Configuration
# ===========================================
# Orígenes permitidos (separados por coma en producción)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ===========================================
# Rate Limiting
# ===========================================
# Ventana de tiempo en milisegundos (15 minutos por defecto)
RATE_LIMIT_WINDOW_MS=900000

# Máximo de peticiones por ventana
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# Security Settings
# ===========================================
# Máxima longitud de mensaje en caracteres
MAX_MESSAGE_LENGTH=500

# Límite de tamaño de JSON body
JSON_BODY_LIMIT=200kb

# Mensajes por minuto por usuario en Socket.IO
SOCKET_MESSAGE_LIMIT=10

# Ventana de mensajes en milisegundos (1 minuto)
SOCKET_MESSAGE_WINDOW=60000

# ===========================================
# Database Configuration (for future use)
# ===========================================
# DATABASE_URL=mongodb://localhost:27017/unachat

# ===========================================
# Logging Configuration
# ===========================================
# LOG_LEVEL=info
# LOG_FILE=./logs/app.log